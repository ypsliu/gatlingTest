{
  "documentId": "${documentId}",
  "documentType": "${documentType}",
  "name": {
    "firstName": "${firstname}",
    "middleName": "${middleName}",
    "surname": "${surname}",
    "title": "${title}"
  },
  "smsAuthentication": {
    "authCode": "${authCode}",
    "mobile": "${mobile}"
  }
}